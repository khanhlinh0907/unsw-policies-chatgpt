<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/unsw_yellow" />

    <!-- Border color and width -->
    <stroke
        android:width="3dp"
        android:color="@color/black" />

    <!-- Rounded corners -->
    <corners android:radius="50dp" />
</shape>
